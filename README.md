# music

Lattice output. Each song folder holds its source — `beat.json` regenerates
the beat exactly, `beat.mid` opens anywhere — plus notes. Rendered audio
stays out of git: `renders/` is scratch, keepers go to the drive.

    songs/<yyyy-mm-dd>-<slug>/
      beat.json
      beat.mid
      notes.md
